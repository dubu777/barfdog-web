import { style } from "@vanilla-extract/css";

export const signUpTermsContainer = style({
	padding: '40px 0',
})

export const signUpTermsList = style({})

export const allAgreement = style({
	marginBottom: '16px'
})

export const subAgreement = style({
	marginLeft: '30px',
})

export const agreementList = style({
	display: 'flex',
	flexDirection: 'column',
	gap: '16px',
})

export const agreementCheckbox = style({
	display: 'flex',
	justifyContent: 'space-between',
	alignItems: 'center',
})

export const termsModal = style({
	cursor: 'pointer'
})

export const termsModalContainer = style({})
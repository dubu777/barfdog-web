import { style } from "@vanilla-extract/css";

export const DeliveryAddressContentWrapper = style({
  display: "flex",
  flexDirection: "column",
  gap: "12px",
  alignItems: 'flex-start',
  justifyContent: 'center',
})
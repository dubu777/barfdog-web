import { style } from "@vanilla-extract/css";

export const heroImageList = style({
  width: '100%',
  minHeight: '560px',
  marginBottom: '20px'
})

export const imageSlideBox = style({
  height: '100%',
})

export const imageSlide = style({
  objectFit: 'contain',
})

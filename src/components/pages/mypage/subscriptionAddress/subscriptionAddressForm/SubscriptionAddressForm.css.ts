import {style} from "@vanilla-extract/css";

export const addressForm = style({
  width: '100%',
  height: '100%',
  marginBottom: '23px',
});

export const addressFormTitle = style({
  lineHeight: '25px !important',
  marginBottom: '50px',

});

export const confirmModal = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '10px',
  lineHeight: 1.3,
});
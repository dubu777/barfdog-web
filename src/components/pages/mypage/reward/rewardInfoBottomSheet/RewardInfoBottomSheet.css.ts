import { style } from "@vanilla-extract/css";

export const rewardInfoTitle = style({
  padding: '20px',
})

export const rewardInfoContents = style({
  padding: '0 20px 12px',
  whiteSpace: 'pre-line',
  textAlign: 'left',
  display: 'flex',
  flexDirection: 'column',
  gap: '8px',
})
export const rewardSubInfo = style({
  paddingLeft: '16px',
})
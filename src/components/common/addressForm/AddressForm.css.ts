import {style} from "@vanilla-extract/css";

export const addressInput = style({
  marginBottom: '33px',
});

export const addressLabel = style({
  width: '100%',
  textAlign: 'left',
  marginBottom: '13px'
});
